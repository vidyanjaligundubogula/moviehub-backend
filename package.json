{
  "name": "moviehub-backend",
  "version": "1.0.0",
  "description": "MovieHub JSON Server Backend",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}